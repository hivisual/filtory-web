---
const features = [
  { icon: "◈", title: "Preset Eksklusif", desc: "Luxra, Laurel, Index — seluruh koleksi eksklusif langsung bisa diakses." },
  { icon: "◉", title: "Grain Film Realistis", desc: "Tekstur film analog yang terasa nyata. Bukan filter digital murahan." },
  { icon: "◐", title: "Light Leak & Dust", desc: "Efek optical yang membuat foto terasa seperti hasil scan negatif sungguhan." },
  { icon: "❖", title: "Export Tanpa Batas", desc: "Ekspor foto resolusi penuh kapan saja, sebanyak yang kamu mau." },
];

const reviews = [
  { name: "s*****a", stars: 5, text: "Adminnya responsif, beli jam 12 malam tetep dilayani. Preset-nya bagus semua." },
  { name: "r*****i", stars: 5, text: "worth it bangeet, beda banget presetnya dari versi gratis. Foto jadi keliatan mahal." },
  { name: "d*****n", stars: 5, text: "Udah langganan 3 bulan, sejauh nggak ada masalah. Aktivasi juga gampang" },
  { name: "a*****y", stars: 5, text: "App underrated yang ga banyak orang tau. recommended banget buat konten creator." },
];

const faqs = [
  { q: "Apakah ini aman?", a: "Aman kak. kami sudah menjual ke ratusan pembeli dan dan sejauh ini aman aman saja. Kami juga ada garansi selama masa berlangganan kak" },
  { q: "Proses aktivasinya bagaimana?", a: "Setelah pembayaran, kami kirim panduan aktivasi step-by-step via WhatsApp/chat Shopee. Prosesnya simpel, bisa selesai dalam 5 menit." },
  { q: "Kalau ganti iPhone atau reset, akses hilang?", a: "Tidak hilang. silakan hubungi kami kembali untuk aktivasi ulang" },
  { q: "Kenapa harga di website berbeda dengan Shopee?", a: "Pembelian via website menggunakan metode direct transfer (QRIS) sehingga memotong biaya admin marketplace. Transaksi via Shopee menyesuaikan dengan potongan biaya admin platform." },
  { q: "Berapa lama akses aktif?", a: "Tersedia pilihan masa aktif 6 bulan dan 10 bulan. Silakan pilih sesuai yang ada butuhkan." },
];
---

<html lang="id">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Labbet Premium - Filtory</title>
  <meta name="description" content="Edit foto yang terasa seperti kenangan dengan Labbet Premium." />
  
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Playfair+Display:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">
</head>
<body>

  <nav class="navbar">
    <div class="nav-content">
      <a href="/" class="logo">FILTORY</a>
      <a href="https://wa.me/62816957050" target="_blank" class="nav-btn">BELI SEKARANG</a>
    </div>
  </nav>

  <section class="hero-section">
    <div class="hero-container">
      
      <img src="/images/logo-labbet.png" alt="Labbet" class="hero-logo" />
      
      <h1 class="massive-title">LABBET PREMIUM</h1>
      <p class="hero-desc">Akses penuh seluruh fitur premium Labbet App. Preset film eksklusif, workflow tanpa batas.</p>
      
      <div class="cta-group">
        <a href="https://wa.me/62816957050" target="_blank" class="btn btn-black">ORDER VIA WA — MULAI RP39.000</a>
        <a href="https://s.shopee.co.id/1gG3IthAYI" target="_blank" class="btn btn-outline">CHECKOUT VIA SHOPEE</a>
      </div>
    </div>
  </section>

  <section class="marquee-section border-bottom">
    <div class="marquee-track">
      <span class="marquee-items">
        <span>100+ PELANGGAN ALL PLATFORM</span>
        <span>5.0★ RATING SHOPEE</span>
        <span>BERGARANSI</span>
        <span>&lt;10 MENIT AKTIVASI</span>
        <span>Proses Mudah</span>
      </span>
      <span class="marquee-items" aria-hidden="true">
        <span>500+ PELANGGAN</span>
        <span>4.9★ RATING SHOPEE</span>
        <span>PLATFORM iOS</span>
        <span>&lt;10 MNT AKTIVASI</span>
      </span>
    </div>
  </section>

  <section class="content-section border-bottom">
    <div class="section-grid">
      <div class="section-title-col">
        <p class="label-text">APA YANG KAMU DAPAT</p>
        <h2 class="large-heading">SEMUA TOOLS UNTUK FOTO YANG TERASA MAHAL.</h2>
      </div>
      <div class="section-content-col features-grid">
        {features.map((f) => (
          <div class="feature-card">
            <div class="feature-icon">{f.icon}</div>
            <h3 class="feature-title">{f.title}</h3>
            <p class="feature-desc">{f.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="content-section border-bottom">
    <div class="section-grid">
      <div class="section-title-col">
        <p class="label-text">PERBANDINGAN</p>
        <h2 class="large-heading">SEBERAPA BEDA VERSI PREMIUM?</h2>
      </div>
      <div class="section-content-col">
        <div class="comparison-table">
          <div class="table-row table-head">
            <div class="col-feature">Fitur</div>
            <div class="col-free">Gratis</div>
            <div class="col-premium">✦ Premium</div>
          </div>
          <div class="table-row">
            <div class="col-feature">Preset & Edits eksklusif</div>
            <div class="col-free text-muted">Terbatas</div>
            <div class="col-premium font-bold">Semua terbuka</div>
          </div>
          <div class="table-row">
            <div class="col-feature">Texture & grain film analog</div>
            <div class="col-free text-muted">Tidak ada</div>
            <div class="col-premium font-bold">Penuh</div>
          </div>
          <div class="table-row">
            <div class="col-feature">Light leak & dust</div>
            <div class="col-free text-muted">Tidak ada</div>
            <div class="col-premium font-bold">Tersedia</div>
          </div>
          <div class="table-row">
            <div class="col-feature">Export resolusi tinggi</div>
            <div class="col-free text-muted">Dibatasi</div>
            <div class="col-premium font-bold">Tak terbatas</div>
          </div>
          <div class="table-row">
            <div class="col-feature">Border film & negative</div>
            <div class="col-free text-muted">Terbatas</div>
            <div class="col-premium font-bold">Semua tersedia</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="content-section border-bottom">
    <div class="section-grid">
      <div class="section-title-col">
        <p class="label-text">CARA PESAN</p>
        <h2 class="large-heading">PILIH YANG PALING NYAMAN BUATMU.</h2>
      </div>
      <div class="section-content-col steps-grid">
        <div class="step-card bg-light wa-card">
          <div class="card-content-wrapper">
            <div class="step-header">
              <span class="step-num">01</span>
              <span class="step-badge">Lebih hemat</span>
            </div>
            
            <h3 class="step-title">ORDER VIA WHATSAPP</h3>

            <ul class="step-list">
              <li>Klik tombol "Order via WhatsApp"</li>
              <li>Kami akan memberikan panduan order</li>
              <li>Bayar via QRIS/Media pembayaran lain</li>
              <li>Terima panduan aktivasi</li>
            </ul>
            
            <div class="pricing-block">
              <div class="price-row">
                <span class="price-dur">6 BULAN</span>
                <span class="price-val">RP39.000</span>
              </div>
              <div class="price-row">
                <span class="price-dur">10 BULAN</span>
                <span class="price-val">RP49.000</span>
              </div>
            </div>

            <a href="https://wa.me/62816957050" class="btn btn-black w-full mt-auto">ORDER VIA WHATSAPP →</a>
          </div>
        </div>

        <div class="step-card bg-light shopee-card">
          <div class="card-content-wrapper">
            <div class="step-header">
              <span class="step-num">02</span>
              <span class="step-badge">Via Shopee</span>
            </div>
            
            <h3 class="step-title">ORDER VIA SHOPEE</h3>

            <ul class="step-list">
              <li>Klik tombol "Checkout Via Shopee"</li>
              <li>Checkout seperti biasa</li>
              <li>Panduan aktivasi dikirim via chat Shopee</li>
            </ul>

            <div class="pricing-block flex-center">
              <span class="shopee-price-text"></span>
            </div>

            <a href="https://s.shopee.co.id/1gG3IthAYI" class="btn btn-outline w-full mt-auto">CHECKOUT VIA SHOPEE →</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="content-section border-bottom">
    <div class="section-grid">
      <div class="section-title-col">
        <p class="label-text">DARI PEMBELI KAMI</p>
        <h2 class="large-heading">APA KATA MEREKA SETELAH PAKAI.</h2>
      </div>
      <div class="section-content-col reviews-grid">
        {reviews.map((r) => (
          <div class="review-card">
            <div class="review-stars">{'★'.repeat(r.stars)}</div>
            <p class="review-text">"{r.text}"</p>
            <div class="review-author">
              <span class="author-name">{r.name}</span>
              <span class="author-source">Pembeli via WA</span>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="content-section">
    <div class="section-grid">
      <div class="section-title-col">
        <p class="label-text">PERTANYAAN UMUM</p>
        <h2 class="large-heading">SEBELUM KAMU BERTANYA.</h2>
      </div>
      <div class="section-content-col">
        <div class="faq-list">
          {faqs.map((item) => (
            <div class="faq-item">
              <h3 class="faq-q">{item.q}</h3>
              <p class="faq-a">{item.a}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <section class="footer-cta-section bg-black text-white">
    <div class="footer-cta-content">
      <h2 class="massive-title text-white">UBAH FOTO BIASA JADI SEPERTI HASIL KAMERA FILM ESTETIK BERKUALITAS</h2>
      <p class="footer-cta-desc">Aktivasi dalam 10 menit. Ratusan kreator sudah membuktikannya.</p>
      <div class="cta-group justify-center">
        <a href="https://wa.me/62816957050" class="btn btn-white">ORDER VIA WA — MULAI 39K</a>
        <a href="https://s.shopee.co.id/1gG3IthAYI" class="btn btn-outline-white">CHECKOUT VIA SHOPEE</a>
      </div>
    </div>
  </section>

  <footer class="main-footer">
    <div class="footer-bottom">
      <p>© 2026 Filtory.</p>
      <p class="align-right">Labbet Premium Access • Indonesia</p>
    </div>
  </footer>

</body>

<style>
  :root {
    --black: #000000;
    --white: #FFFFFF;
    --light: #F5F5F5;
    --border: #E5E5E5;
    --text-muted: #737373;
  }

  * { box-sizing: border-box; }

  body {
    margin: 0;
    padding: 0;
    font-family: 'Inter', -apple-system, sans-serif;
    background-color: var(--white);
    color: var(--black);
    -webkit-font-smoothing: antialiased;
    line-height: 1.5;
  }

  /* Utility Classes */
  .bg-black { background-color: var(--black); }
  .bg-light { background-color: var(--light); }
  .text-white { color: var(--white) !important; }
  .text-muted { color: var(--text-muted); }
  .font-bold { font-weight: 600; }
  .w-full { width: 100%; text-align: center; }
  .justify-center { justify-content: center; }
  .border-bottom { border-bottom: 1px solid var(--border); }
  .mt-auto { margin-top: auto; }

  /* Navigation */
  .navbar {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 50;
    background-color: var(--white);
    border-bottom: 1px solid var(--border);
    padding: 16px 24px;
  }

  .nav-content { display: flex; justify-content: space-between; align-items: center; }
  .logo { font-size: 1.25rem; font-weight: 600; letter-spacing: -0.02em; text-decoration: none; color: var(--black); }
  .nav-btn { font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.05em; background-color: var(--black); color: var(--white); padding: 10px 20px; text-decoration: none; }

  /* Hero */
  .hero-section {
    margin-top: 60px;
    padding: 100px 24px;
    text-align: center;
    border-bottom: 1px solid var(--border);
  }

  .hero-container { max-width: 1000px; margin: 0 auto; }
  
  .hero-logo {
    height: clamp(80px, 15vw, 180px);
    width: auto;
    max-width: 100%;
    margin: 0 auto 32px auto;
    display: block;
    object-fit: contain;
  }

  .massive-title {
    font-family: 'Playfair Display', serif;
    font-size: clamp(3rem, 10vw, 7rem);
    line-height: 0.9;
    letter-spacing: -0.02em;
    font-weight: 400;
    text-transform: uppercase;
    margin: 0 0 32px 0;
  }

  .hero-desc {
    font-size: clamp(1rem, 3vw, 1.25rem);
    color: var(--text-muted);
    max-width: 600px;
    margin: 0 auto 48px auto;
  }

  /* Buttons */
  .cta-group { display: flex; gap: 16px; flex-wrap: wrap; justify-content: center; }
  
  .btn {
    display: inline-block;
    padding: 16px 32px;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    text-decoration: none;
    transition: all 0.2s;
  }

  .btn-black { background-color: var(--black); color: var(--white); }
  .btn-outline { background-color: transparent; color: var(--black); border: 1px solid var(--black); }
  .btn-white { background-color: var(--white); color: var(--black); }
  .btn-outline-white { background-color: transparent; color: var(--white); border: 1px solid var(--white); }
  
  .btn:hover { opacity: 0.8; }

  /* Marquee Section */
  .marquee-section {
    padding: 24px 0;
    overflow: hidden;
    background-color: var(--white);
  }

  .marquee-track {
    display: flex;
    white-space: nowrap;
    font-family: 'Playfair Display', serif;
    font-size: clamp(2.5rem, 8vw, 5rem);
    line-height: 1;
    letter-spacing: -0.02em;
    font-weight: 400;
    width: fit-content;
    animation: marquee 20s linear infinite;
  }

  .marquee-items {
    display: inline-flex;
    gap: 1.5em;
    padding-right: 1.5em;
  }

  @keyframes marquee {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  /* Universal Content Layout */
  .content-section { padding: 80px 24px; }
  .section-grid { display: grid; grid-template-columns: 1fr; gap: 48px; max-width: 1200px; margin: 0 auto; }
  
  .label-text {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-muted);
    margin: 0 0 16px 0;
  }

  .large-heading {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2rem, 5vw, 3.5rem);
    line-height: 1;
    letter-spacing: -0.02em;
    font-weight: 400;
    text-transform: uppercase;
    margin: 0;
  }

  /* Features Grid - Mobile First */
  .features-grid { 
    display: grid; 
    grid-template-columns: repeat(2, 1fr);
    gap: 24px 16px;
  }
  
  .feature-icon { font-size: 1.5rem; margin-bottom: 8px; }
  .feature-title { font-size: 1rem; font-weight: 600; text-transform: uppercase; margin: 0 0 8px 0; }
  .feature-desc { color: var(--text-muted); font-size: 0.85rem; margin: 0; }

  /* Comparison Table */
  .comparison-table { width: 100%; border-top: 2px solid var(--black); }
  .table-row { display: grid; grid-template-columns: 2fr 1fr 1fr; padding: 20px 0; border-bottom: 1px solid var(--border); }
  .table-head { font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.05em; color: var(--text-muted); border-bottom: 2px solid var(--black); }

  /* Steps Grid */
  .steps-grid { display: grid; grid-template-columns: 1fr; gap: 24px; }
  
  .step-card { 
    padding: 32px; 
    border: 1px solid var(--border); 
    display: flex; 
    flex-direction: column; 
    position: relative; /* Wajib untuk absolute background */
    overflow: hidden; /* Agar logo tidak keluar box */
  }

  /* Wrapper konten agar berada di atas background logo */
  .card-content-wrapper {
    position: relative;
    z-index: 2;
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  /* Styling pseudo-element untuk background logo WA */
  .wa-card::before {
    content: ' ';
    position: absolute;
    inset: 0;
    z-index: 1;
    background-image: url('/images/icon-wa.svg');
    background-repeat: no-repeat;
    background-position: center;
    background-size: 80%; /* Ukuran logo di background */
    opacity: 0.08; /* Opacity sangat rendah */
  }

  /* Styling pseudo-element untuk background logo Shopee */
  .shopee-card::before {
    content: ' ';
    position: absolute;
    inset: 0;
    z-index: 1;
    background-image: url('/images/icon-shopee.svg');
    background-repeat: no-repeat;
    background-position: center;
    background-size: 80%; /* Ukuran logo di background */
    opacity: 0.08; /* Opacity sangat rendah */
  }

  .step-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 32px; border-bottom: 1px solid var(--border); padding-bottom: 16px; }
  .step-num { font-family: 'Playfair Display', serif; font-size: 2rem; }
  .step-badge { font-size: 0.65rem; text-transform: uppercase; letter-spacing: 0.1em; background: var(--black); color: var(--white); padding: 4px 8px; }
  
  .step-title { font-size: 1.25rem; margin: 0 0 16px 0; text-transform: uppercase; }

  .step-list { margin: 0 0 32px 0; padding-left: 20px; color: var(--text-muted); }
  .step-list li { margin-bottom: 8px; }

  /* Pricing Block in Cards */
  .pricing-block { margin-bottom: 32px; border-top: 1px solid var(--border); padding-top: 16px; }
  .price-row { display: flex; justify-content: space-between; align-items: center; padding: 8px 0; }
  .price-val { font-size: 1.5rem; font-weight: 600; }
  .price-dur { font-size: 0.85rem; font-weight: 500; color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.05em; }
  
  .flex-center { display: flex; align-items: center; justify-content: center; height: 100%; min-height: 80px; }
  .shopee-price-text { font-size: 1rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; color: var(--text-muted); }

  /* Reviews */
  .reviews-grid { display: grid; grid-template-columns: 1fr; gap: 24px; }
  .review-card { padding: 32px; border: 1px solid var(--border); }
  .review-stars { font-size: 1.25rem; letter-spacing: 0.1em; margin-bottom: 16px; }
  .review-text { font-family: 'Playfair Display', serif; font-size: 1.5rem; line-height: 1.3; margin: 0 0 24px 0; }
  .review-author { display: flex; flex-direction: column; font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.05em; }
  .author-source { color: var(--text-muted); margin-top: 4px; }

  /* FAQ */
  .faq-list { border-top: 2px solid var(--black); }
  .faq-item { padding: 32px 0; border-bottom: 1px solid var(--border); }
  .faq-q { font-size: 1.25rem; font-weight: 600; margin: 0 0 12px 0; }
  .faq-a { color: var(--text-muted); margin: 0; }

  /* Footer CTA */
  .footer-cta-section { padding: 120px 24px; text-align: center; }
  .footer-cta-content { max-width: 900px; margin: 0 auto; }
  .footer-cta-desc { color: #A3A3A3; margin: 0 auto 48px auto; font-size: 1.25rem; }

  /* Footer Bottom */
  .main-footer { padding: 24px; border-top: 1px solid var(--border); }
  .footer-bottom { display: flex; flex-direction: column; gap: 8px; font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.05em; color: var(--text-muted); }

  /* Desktop Adjustments */
  @media (min-width: 768px) {    
    .section-grid { grid-template-columns: 1fr 2fr; }
    .section-title-col { position: sticky; top: 100px; align-self: start; }
    
    /* Revert features back to larger size on desktop */
    .features-grid { grid-template-columns: repeat(2, 1fr); gap: 40px; }
    .feature-icon { font-size: 2rem; margin-bottom: 16px; }
    .feature-title { font-size: 1.25rem; }
    .feature-desc { font-size: 1rem; }

    .steps-grid { grid-template-columns: repeat(2, 1fr); }
    .reviews-grid { grid-template-columns: repeat(2, 1fr); }
    
    .footer-bottom { flex-direction: row; justify-content: space-between; }
    .align-right { text-align: right; }
  }
</style>
</html>